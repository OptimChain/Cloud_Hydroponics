# import relevant libary
import RPi.GPIO as GPIO

# set mode to BCM GPIO numbering
GPIO.setmode(GPIO.BCM)

# Sets input pin, pin number depends on the layout/maybe it is arbitary?
INPUT_PIN = 4          
GPIO.setup(INPUT_PIN, GPIO.IN)  # Set input pin to be an input

#define variable
float flow_rate = 0

# Create a function to run when the input is high
def inputhigh(channel):
    # similar cal logic with the original signal function
    
# Wait for the input to go low, run the function when it does
GPIO.add_event_detect(INPUT_PIN, GPIO.RISING, callback=inputhigh, bouncetime=200) 

# Start a loop that never ends
while True:
    # update float rate calculation
    sleep(1);           # Sleep for a full second before restarting our loop

# clean up
GPIO.cleanup()